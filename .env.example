# Obol reads these only for the optional accuracy/history tiers.
# Local diagnosis works with NO keys at all.
ANTHROPIC_API_KEY=        # optional: exact counts via the free count_tokens endpoint
ANTHROPIC_ADMIN_KEY=      # optional: real history via the free Usage & Cost Admin API
